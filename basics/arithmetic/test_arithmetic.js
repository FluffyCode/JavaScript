// Playing around with maths in JavaScript

// Exponents
// The first argument is the base number; the second is the exponent
  document.write("<p class='h'>Exponents</p>");
  document.write("<p class='i'>Math.pow(3,2) = " + Math.pow(3,2) + "</p>");
    // => 9
  document.write("<p class='i'>Math.pow(4,3) = " + Math.pow(4,3) + "</p>");
    // => 64

document.write("<br>");

// Rounding, rounding up and rounding down
  document.write("<p class='h'>Rounding</p>");
  document.write("<p class='i'>Math.round(.4) = " + Math.round(.4) + "</p>")
    // => 0
  document.write("<p class='i'>Math.round(.5) = " + Math.round(.5) + "</p>")
    // => 1
  document.write("<p class='i'>Math.round(.6) = " + Math.round(.6) + "</p>")
    // => 1
